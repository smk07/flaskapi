create table users(
    userid char(7),
    username varchar(30),
    email text,
    pass text,
    primary key(userid)
);

create table liked(
    userid char(7),
    title varchar(50),
    foreign key (userid)
);